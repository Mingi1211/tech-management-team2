# Supabase 대시보드 → Project Settings → API 에서 복사한다.
# .env.local 파일을 만들어 여기에 실제 값을 넣는다. .env.local 은 커밋되지 않는다.

SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# anon key 를 쓴다. RLS 가 "응답 넣기만 허용" 으로 걸려 있어 이 키로는 남의 응답을 읽지 못한다.
SUPABASE_ANON_KEY=eyJhbGciOi...

# service_role key 는 절대 여기에도, 레포에도 넣지 않는다.
